---
phase: 03-posts-+-profiles
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/server/posts/mod.rs
  - src/server/posts/handlers.rs
  - src/server/mod.rs
  - src/lib.rs
autonomous: true
requirements:
  - POST-01
  - POST-02
  - POST-03
  - PROF-01
  - PROF-02

must_haves:
  truths:
    - "`create_post` rejects empty content and content over 280 characters"
    - "`delete_post` with a mismatched user_id returns an error (rows_affected == 0 path)"
    - "`PostWithAuthor` serializes and deserializes correctly (available in WASM)"
    - "`relative_timestamp` formats all four time ranges correctly (s/m/h/d)"
    - "Server functions are registered and visible to the Leptos router"
  artifacts:
    - path: "src/server/posts/handlers.rs"
      provides: "create_post, delete_post, list_own_posts, get_user_posts server functions + PostWithAuthor struct + relative_timestamp utility + unit tests"
      exports:
        - create_post
        - delete_post
        - list_own_posts
        - get_user_posts
        - PostWithAuthor
        - relative_timestamp
    - path: "src/server/posts/mod.rs"
      provides: "posts submodule definition"
    - path: "src/server/mod.rs"
      provides: "pub mod posts added"
  key_links:
    - from: "src/server/posts/handlers.rs"
      to: "PgPool context"
      via: "use_context::<PgPool>()"
      pattern: "use_context::<PgPool>"
    - from: "PostWithAuthor"
      to: "WASM compilation"
      via: "defined outside #[cfg(feature = \"ssr\")] — no ssr gate on the struct"
      pattern: "pub struct PostWithAuthor"
---

<objective>
Define all server-side data types and server functions for posts, with failing tests written first.

Purpose: Establish the typed contract that all UI components in Phase 3 build against. Running tests first ensures the logic is correct before any UI consumes it.
Output: `src/server/posts/` module with `create_post`, `delete_post`, `list_own_posts`, `get_user_posts`, `PostWithAuthor`, `relative_timestamp`, and passing unit tests.
</objective>

<execution_context>
@/Users/robinsladky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/robinsladky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-posts-+-profiles/03-CONTEXT.md
@.planning/phases/03-posts-+-profiles/03-RESEARCH.md

<interfaces>
<!-- Existing types the executor needs. No exploration required. -->

From src/auth_user.rs:
```rust
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct AuthUser {
    pub id: i32,
    pub username: String,
}
```

From src/server/auth/handlers.rs (established server function pattern):
```rust
#[leptos::server]
pub async fn register(email: String, password: String) -> Result<String, leptos::prelude::ServerFnError> {
    use sqlx::PgPool;
    let pool = leptos::prelude::use_context::<PgPool>()
        .ok_or_else(|| leptos::prelude::ServerFnError::new("Database not available"))?;
    // ... ssr-gated query code ...
}
```

From src/server/mod.rs (current):
```rust
pub mod auth;
pub mod error;
```

From src/lib.rs (current — server fn registration):
```rust
// server functions are registered via leptos_axum::generate_route_list
// which picks up all #[leptos::server] functions in scope
```

Established pitfall (from RESEARCH.md):
- `PostWithAuthor` struct MUST be defined OUTSIDE `#[cfg(feature = "ssr")]` — it must compile in WASM
- Only the DB query body inside server functions should be ssr-gated (they are implicitly via `#[leptos::server]`)
- `delete_post` authorization: use `WHERE id = $1 AND author_id = $2` + `rows_affected() == 0` check
- `user_id` for mutations comes from the client (from `auth_user` context signal) — safe because the DB check is authoritative
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Write failing tests for post server logic</name>
  <files>src/server/posts/mod.rs, src/server/posts/handlers.rs, src/server/mod.rs</files>
  <behavior>
    - create_post_rejects_empty: calling validation logic with empty string returns Err containing "cannot be empty"
    - create_post_rejects_over_limit: calling validation logic with a 281-char string returns Err containing "280"
    - delete_post_rejects_wrong_user: when rows_affected == 0, function returns Err containing "Not authorized"
    - post_with_author_serde_roundtrip: PostWithAuthor can be serialized to JSON and deserialized back with identical fields
    - relative_timestamp_formats: seconds < 60 → "Xs", minutes < 60 → "Xm", hours < 24 → "Xh", days → "Xd"
    - user_posts_order: a list of PostWithAuthor sorted by created_at DESC has the newest first (test the sort ordering contract)
  </behavior>
  <action>
    Create `src/server/posts/mod.rs`:
    ```rust
    pub mod handlers;
    pub use handlers::*;
    ```

    Add `pub mod posts;` to `src/server/mod.rs` (after `pub mod auth;`).

    Create `src/server/posts/handlers.rs` with:

    1. `PostWithAuthor` struct — OUTSIDE any `#[cfg(feature = "ssr")]` block, derives `Clone, Debug, Serialize, Deserialize`:
       ```rust
       #[derive(Clone, Debug, Serialize, Deserialize)]
       pub struct PostWithAuthor {
           pub id: i32,
           pub author_id: i32,
           pub author_username: String,
           pub content: String,
           pub created_at: chrono::NaiveDateTime,
           pub relative_time: String,
       }
       ```
       Include the `relative_time: String` field — compute it server-side so UI components can display it without chrono arithmetic.

    2. `relative_timestamp(created_at: NaiveDateTime) -> String` — public, not ssr-gated, uses `chrono::Utc::now().naive_utc()`:
       - diff.num_seconds() < 60 → format!("{}s", diff.num_seconds())
       - diff.num_minutes() < 60 → format!("{}m", diff.num_minutes())
       - diff.num_hours() < 24 → format!("{}h", diff.num_hours())
       - else → format!("{}d", diff.num_days())

    3. Four server functions (all `#[leptos::server]`, ssr body only):
       - `create_post(content: String, user_id: i32) -> Result<(), ServerFnError>`: trim content, reject empty (Err "Post cannot be empty"), reject > 280 chars (Err "Post exceeds 280 characters"), INSERT into posts
       - `delete_post(post_id: i32, user_id: i32) -> Result<(), ServerFnError>`: DELETE WHERE id=$1 AND author_id=$2, check rows_affected() == 0 → Err "Not authorized or post not found"
       - `list_own_posts(user_id: i32) -> Result<Vec<PostWithAuthor>, ServerFnError>`: SELECT ... FROM posts JOIN users ON author_id=users.id WHERE p.author_id=$1 ORDER BY p.created_at DESC, p.id DESC — populate relative_time via relative_timestamp()
       - `get_user_posts(username: String) -> Result<Vec<PostWithAuthor>, ServerFnError>`: SELECT ... WHERE u.username=$1 ORDER BY p.created_at DESC, p.id DESC — populate relative_time via relative_timestamp()

    4. `#[cfg(test)] mod tests` at the bottom of handlers.rs — write tests that FAIL before implementation:
       For create/delete validation, test the logic directly (extract validation into an inner fn or test the error condition from the public surface). For `relative_timestamp`, call it directly. For `PostWithAuthor` serde, construct a value and round-trip through `serde_json::to_string` + `serde_json::from_str`.

    IMPORTANT: Run `cargo test --features ssr` after writing stubs and tests to confirm tests fail (RED phase). Then implement the logic until tests pass (GREEN phase). Commit after GREEN.

    Feature-gating reminder: only the DB query code inside server functions runs under ssr — `PostWithAuthor` and `relative_timestamp` must compile in both ssr and hydrate (no cfg gates on them).
  </action>
  <verify>
    <automated>cargo test --features ssr 2>&1 | tail -20</automated>
  </verify>
  <done>
    `cargo test --features ssr` exits 0 with all 6 tests passing:
    - post::tests::create_post_rejects_empty
    - post::tests::create_post_rejects_over_limit
    - post::tests::delete_post_rejects_wrong_user
    - post::tests::post_with_author_serde_roundtrip
    - post::tests::relative_timestamp_formats
    - post::tests::user_posts_order
    No test failures. No compiler warnings for the new module.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire posts module into lib.rs and verify full build</name>
  <files>src/lib.rs</files>
  <action>
    Open `src/lib.rs`. Find where `server::auth` functions are registered or imported for the Leptos router. Ensure `server::posts` functions are also in scope so `generate_route_list` picks them up.

    In Leptos 0.8, server functions register themselves via the `#[leptos::server]` macro at compile time — no explicit `use` import is required in lib.rs. However, the module must be reachable from the crate root. Verify that:
    1. `src/server/mod.rs` now has `pub mod posts;`
    2. `src/lib.rs` has `pub mod server;` (already present — do not duplicate)
    3. If lib.rs imports specific server functions via `use crate::server::auth::...`, add matching `use crate::server::posts::*;` — but only if needed for compilation

    Run `cargo build --features ssr` and fix any compilation errors. The most common issue will be unused import warnings — fix with `#[allow(unused_imports)]` only if necessary, prefer removing the import.

    Also run `cargo build` (without --features ssr, i.e., the hydrate/WASM build) to confirm `PostWithAuthor` compiles in WASM context. If this fails with "cannot find type PostWithAuthor", it means the struct was accidentally placed inside a `#[cfg(feature = "ssr")]` block — move it out.
  </action>
  <verify>
    <automated>cargo build --features ssr 2>&1 | grep -E "^error" | head -20 && cargo test --features ssr 2>&1 | tail -5</automated>
  </verify>
  <done>
    `cargo build --features ssr` exits 0 with no errors (warnings acceptable).
    `cargo test --features ssr` exits 0 with all 6 post tests passing.
    `cargo build` (WASM/hydrate) exits 0 — PostWithAuthor is WASM-accessible.
  </done>
</task>

</tasks>

<verification>
Run full test suite: `cargo test --features ssr`
Expected: All previously passing auth tests still pass + 6 new post tests pass.
No regressions in Phase 2 functionality.
</verification>

<success_criteria>
- `src/server/posts/` module exists with handlers.rs and mod.rs
- `src/server/mod.rs` exports `pub mod posts;`
- `PostWithAuthor` struct compiles in both ssr and hydrate (WASM) builds
- `relative_timestamp` is a public non-ssr-gated function
- All 4 server functions exist: create_post, delete_post, list_own_posts, get_user_posts
- 6 unit tests pass under `cargo test --features ssr`
- Zero build errors in both `cargo build --features ssr` and `cargo build`
</success_criteria>

<output>
After completion, create `.planning/phases/03-posts-+-profiles/03-01-SUMMARY.md`
</output>
