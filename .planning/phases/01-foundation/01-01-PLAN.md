---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - Cargo.lock
  - src/lib.rs
  - src/app.rs
  - src/bin/server/main.rs
  - end2end/tests/example.spec.ts
  - .gitignore
  - Leptos.toml
autonomous: false
requirements: []

must_haves:
  truths:
    - "`cargo leptos watch` starts without panic and serves an HTML response at localhost:3000"
    - "The project compiles cleanly with `cargo build` (online mode, DB available)"
    - "The start-axum template structure exists: src/bin/server/main.rs, src/app.rs, src/lib.rs"
  artifacts:
    - path: "Cargo.toml"
      provides: "Workspace definition with [package.metadata.leptos] section and all required dependencies"
      contains: "leptos, axum, sqlx, jsonwebtoken, argon2, serde, tokio"
    - path: "src/bin/server/main.rs"
      provides: "Axum server entry point that starts on 0.0.0.0:3000"
      exports: ["main"]
    - path: "src/app.rs"
      provides: "Root Leptos App component"
      exports: ["App"]
    - path: "Leptos.toml"
      provides: "cargo-leptos build configuration"
  key_links:
    - from: "src/bin/server/main.rs"
      to: "localhost:3000"
      via: "tokio::main + axum Router binding"
      pattern: "bind.*3000"
    - from: "src/app.rs"
      to: "src/bin/server/main.rs"
      via: "leptos_axum::LeptosRoutes"
      pattern: "LeptosRoutes"
---

<objective>
Scaffold the My_X full-stack Rust project using the official `cargo leptos new` start-axum template, then add all Phase 1–5 dependencies to Cargo.toml so every subsequent plan has a clean foundation to build on.

Purpose: All other plans depend on a compilable workspace with the correct crates available. Without this, nothing else can be implemented.
Output: A runnable Leptos + Axum project at localhost:3000, with all v1 dependencies declared in Cargo.toml.
</objective>

<execution_context>
@/Users/robinsladky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/robinsladky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold project with cargo leptos new and add all dependencies</name>
  <files>Cargo.toml, Leptos.toml, src/lib.rs, src/app.rs, src/bin/server/main.rs, .gitignore</files>
  <action>
Run the following commands from the working directory (project root, which is already My_X/):

```bash
# Install cargo-leptos if not already installed
cargo install cargo-leptos --locked

# Scaffold into the CURRENT directory using the start-axum template.
# NOTE: cargo leptos new creates a new subdirectory. Since we're already IN My_X/,
# scaffold into a temp dir then move files up, OR scaffold a fresh directory named my_x
# and copy its contents to the project root.
# PREFERRED approach: scaffold in parent dir then move files here.
cd ..
cargo leptos new --name my_x --git https://github.com/leptos-rs/start-axum
# This creates ../my_x/ — move contents into current project root
cp -r my_x/. My_X/
rm -rf my_x
cd My_X
```

After scaffolding, open `Cargo.toml` and add/verify these dependencies are present with exact versions (add any missing ones):

```toml
[dependencies]
leptos = { version = "0.8.17", features = ["nightly"] }
leptos_axum = { version = "0.8.17" }
axum = { version = "0.8.8", features = ["macros"] }
sqlx = { version = "0.8.6", features = ["runtime-tokio-rustls", "postgres", "chrono", "uuid"] }
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
jsonwebtoken = "10.3.0"
argon2 = "0.5.3"
rand = "0.8"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
chrono = { version = "0.4", features = ["serde"] }
uuid = { version = "1", features = ["v4", "serde"] }
thiserror = "1"
dotenvy = "0.15"
```

Keep all template-generated features/sections intact (especially `[package.metadata.leptos]` and `[[bin]]` sections). Do not remove any template dependencies.

After editing Cargo.toml, verify the project compiles:
```bash
cargo build 2>&1 | tail -20
```

Fix any version conflicts by using the template's versions as baseline and adding missing crates.
  </action>
  <verify>
    <automated>cd /Users/robinsladky/delivery/Perso/My_X && cargo build 2>&1 | grep -E "^error" | head -5; echo "Exit: $?"</automated>
  </verify>
  <done>
`cargo build` completes without errors. All required crates (sqlx, axum, leptos, jsonwebtoken, argon2, tokio) are present in Cargo.toml. The files src/bin/server/main.rs, src/app.rs, src/lib.rs, and Leptos.toml exist.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify server starts and serves HTML</name>
  <action>Human verification: confirm cargo leptos watch starts and localhost:3000 serves HTML without panics.</action>
  <what-built>Full-stack Leptos + Axum project scaffold with all Phase 1-5 crates declared in Cargo.toml. `cargo build` passes.</what-built>
  <how-to-verify>
1. In a terminal, run: `cargo leptos watch` from /Users/robinsladky/delivery/Perso/My_X
2. Wait ~30 seconds for compilation (first build is slow)
3. Open browser at http://localhost:3000
4. Verify you see an HTML page (even if it is just "Welcome to Leptos!" or similar template content)
5. Check terminal for any ERROR or panic lines — there should be none
6. Press Ctrl+C to stop the server
  </how-to-verify>
  <resume-signal>Type "approved" when localhost:3000 serves an HTML page without errors, or describe any issues you see</resume-signal>
</task>

</tasks>

<verification>
- `cargo build` exits 0 with no error lines
- All required crates present in Cargo.toml
- localhost:3000 serves HTML after `cargo leptos watch`
- No panic or thread 'main' errors in output
</verification>

<success_criteria>
The start-axum scaffold is in place, compiles cleanly, and serves a Leptos page at localhost:3000. Every subsequent plan can add files to this workspace without modifying its structure.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` with:
- What was scaffolded (template version, actual crate versions from Cargo.lock)
- Any deviations from the template (added/removed crates)
- Confirmation that localhost:3000 serves HTML
</output>
